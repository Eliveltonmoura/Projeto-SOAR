# URL completa da API do backend (com o prefixo /api/v1).
# Necessário em produção (ex: Vercel), onde não existe o proxy do Vite.
# Em desenvolvimento local, pode deixar vazio — o vite.config.ts já
# redireciona /api para http://localhost:3000.
VITE_API_URL=https://seu-backend.exemplo.com/api/v1
